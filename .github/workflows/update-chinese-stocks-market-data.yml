# ====================================================================
#  中概股高频数据更新工作流 (最终修复版)
#  版本: 4.0 - Corrected Environment Variables
#  目的: 此文件是中概股高频更新的唯一、正确的调度器。
# ====================================================================
name: Update Chinese Stocks Market Data (High Frequency)

on:
  schedule:
    # 中概股更新: 在美股交易时段的每小时 05, 20, 35, 50 分执行
    # UTC 13:00-20:00 覆盖美东夏令时/冬令时 9:00-16:00
    - cron: '5,20,35,50 13-20 * * 1-5'
  workflow_dispatch:
    inputs:
      debug_mode:
        description: 'Enable debug mode for detailed logs? (开启调试日志?)'
        required: true
        default: 'false'
        type: choice
          - 'true'
          - 'false'

jobs:
  update_chinese_stocks:
    name: "🐉 Update Chinese Stocks Data"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        u