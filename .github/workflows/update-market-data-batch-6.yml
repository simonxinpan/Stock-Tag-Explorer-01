# 文件: .github/workflows/update-market-data-batch-6.yml
# 版本: Batch Processing - Batch 6 (Stocks 251-300)
name: Update S&P 500 Market Data - Batch 6 (251-300)

on:
  schedule:
    # 每15分钟开始第六批处理，延迟25分钟 (周一至周五, UTC 13-20点)
    - cron: '25,40,55,10 13-20 * * 1-5'
    # 闭市后最终运行
    - cron: '25 21 * * 1-5'
  workflow_dispatch:
    inputs:
      debug_mode:
        description: 'Enable debug mode for detailed logs?'
        required: true
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

concurrency:
  group: market-data-update-batch-6
  cancel-in-progress: false

jobs:
  update_sp500_batch_6:
    name: "🚀 Update S&P 500 Data - Batch 6 (251-300)"
    runs-on: ubuntu-latest
    timeout-minutes: 8
    
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
      DEBUG: ${{ github.event.inputs.debug_mode || 'false' }}
      BATCH_START: 251
      BATCH_END: 300
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Health check - Verify API and DB connectivity
        run: |
          echo "🔍 Checking Finnhub API connectivity..."
          curl -f "https://finnhub.io/api/v1/quote?symbol=AAPL&token=$FINNHUB_API_KEY" || exit 1
          echo "✅ Finnhub API is accessible"
          
      - name: Run S&P 500 Update Script - Batch 6
        run: |
          echo "🚀 Starting S&P 500 market data update - Batch 6 (stocks 251-300)..."
          echo "📊 Expected runtime: 2-3 minutes for 50 stocks"
          node _scripts/update-market-data.mjs
          
      - name: Verify Update Success
        run: |
          echo "✅ S&P 500 Batch 6 update completed successfully"