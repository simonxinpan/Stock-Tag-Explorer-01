# 文件: .github/workflows/update-market-data-batch-9.yml
# 版本: Batch Processing - Batch 9 (Stocks 401-450)
name: Update S&P 500 Market Data - Batch 9 (401-450)

on:
  schedule:
    # 每15分钟开始第九批处理，延迟40分钟 (周一至周五, UTC 13-20点)
    - cron: '40,55,10,25 13-20 * * 1-5'
    # 闭市后最终运行
    - cron: '40 21 * * 1-5'
  workflow_dispatch:
    inputs:
      debug_mode:
        description: 'Enable debug mode for detailed logs?'
        required: true
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

concurrency:
  group: market-data-update-batch-9
  cancel-in-progress: false

jobs:
  update_sp500_batch_9:
    name: "🚀 Update S&P 500 Data - Batch 9 (401-450)"
    runs-on: ubuntu-latest
    timeout-minutes: 8
    
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
      DEBUG: ${{ github.event.inputs.debug_mode || 'false' }}
      BATCH_START: 401
      BATCH_END: 450
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Health check - Verify API and DB connectivity
        run: |
          echo "🔍 Checking Finnhub API connectivity..."
          curl -f "https://finnhub.io/api/v1/quote?symbol=AAPL&token=$FINNHUB_API_KEY" || exit 1
          echo "✅ Finnhub API is accessible"
          
      - name: Run S&P 500 Update Script - Batch 9
        run: |
          echo "🚀 Starting S&P 500 market data update - Batch 9 (stocks 401-450)..."
          echo "📊 Expected runtime: 2-3 minutes for 50 stocks"
          node _scripts/update-market-data.mjs
          
      - name: Verify Update Success
        run: |
          echo "✅ S&P 500 Batch 9 update completed successfully"